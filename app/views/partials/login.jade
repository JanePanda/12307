div(ng-include='\'partials/navbar.html\'')
div#bread-nav
  p
    |您现在的位置：
    a(href = '\'partials/navbar.html\'')
      |首页 
    span
      |> 登录    
div#login
  div#login-area
    .row
      .col-sm-12
      
          p
            | 注意：当前是开发状态，每次服务器重启，帐号都会重置。初始用户在
            code dummydata.js
            | 中定义. 默认 用户/密码 是
            code test@test.com
            | /
            code test
      
      .col-sm-12
        form.form(name='form', ng-submit='login(form)', novalidate='novalidate')

          .form-group
            label 登录名：
            input.form-control(type='email', name='email', ng-model='user.email', placeholder='邮箱' required )

          .form-group
            label 密码：
            input.form-control(type='password', name='password', ng-model='user.password',placeholder='密码' required)

          .form-group.has-error
            p.help-block(ng-show='form.email.$error.required && form.password.$error.required && submitted')
              | 请输入你注册用的email和密 码
            p.help-block {{ errors.other }}

          button.btn.btn-lg.btn-primary(type='submit')  登录 
          span.clearfix

    .row
      .col-sm-12
        hr
        div#tip-area
          | 还未注册用户？ 
          a.text-center.new-account(href='/signup') 注册新用户

  div#notice-area
    img(src = "../../images/login-notice.PNG")
